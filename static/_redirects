# Cloudflare Pages Redirect Rules
# Note: Cloudflare requires HTTP status 307 (Temporary Redirect) for external target URLs to preserve POST/PUT request bodies.

# To route API calls to an external backend, replace your-backend.hf.space with your backend URL:
# /api/*  https://your-backend.hf.space/api/:splat  307

# Single Page Application (SPA) Client Routing Fallback
/*  /index.html  200
